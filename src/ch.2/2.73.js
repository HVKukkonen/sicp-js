// a. We need to cover cases where the expression doesn't follow the notation where the input is a list with the operator given as the 1st element. 
// This needs to be handled to be compatible with an earlier version that supports such expressions for single element expressions, e.g. deriv("x", "x")

// b. Same functions can be used.

// d. No changes. Changes are limited to the getters and setter.